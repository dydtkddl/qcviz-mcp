# ═══════════════════════════════════════════════════════════════

# QCViz-MCP 전수조사 대응 일괄 실행 프롬프트 v1.0

# 대상: https://github.com/dydtkddl/qcviz-mcp

# 작성일: 2026-03-05

# 모드: 전자동 (오토모드) — 전 Phase를 순차 실행, 중간 보고 없이 최종 보고만

# ═══════════════════════════════════════════════════════════════

## ROLE

너는 계산화학 소프트웨어 엔지니어 + 보안 엔지니어 + JOSS 논문 준비 전문가이다.
전수조사 보고서에서 도출된 18건의 이슈를 한 번에 해결한다.
각 Phase를 순서대로 실행하고, 실패 시 원인을 기록한 뒤 다음 Phase로 넘어간다.
**모든 Phase를 끝낸 후 한 번만 최종 보고를 출력한다.**

## CONTEXT

- 프로젝트 경로: `/mnt/d/20260305_양자화학시각화MCP서버구축/`
- conda 환경: `qcviz` (Python 3.11, PySCF 설치됨)
- 현재 상태: 50개 테스트 통과 (Linux), v0.2.0-alpha 태그, GitHub public
- 참조 문서: `docs/PROJECT_HISTORY.md`, `docs/20260305.2059.히스토리압축.md`
- pyproject.toml에 `pythonpath = ["."]` 추가 필요 (test_benchmark import 에러 수정)

## EXECUTION PLAN

### Phase δ-1: 즉시 보안 패치 [이슈 #6, #15]

**파일: `src/qcviz_mcp/tools/core.py`**

1. `parse_output` 함수 상단에 경로 검증 로직 추가:

```python
import os

ALLOWED_DIRS = [
    os.path.realpath(os.path.join(os.path.dirname(__file__), '..', '..', '..')),  # 프로젝트 루트
]

def _validate_file_path(file_path: str) -> str:
    """경로 탐색 공격 방지를 위한 파일 경로 검증."""
    real_path = os.path.realpath(file_path)
    if not any(real_path.startswith(allowed) for allowed in ALLOWED_DIRS):
        raise ValueError(
            f"보안: 허용되지 않은 경로입니다. "
            f"프로젝트 디렉토리 내부의 파일만 접근 가능합니다."
        )
    if not os.path.isfile(real_path):
        raise FileNotFoundError(f"파일을 찾을 수 없습니다: {file_path}")
    return real_path
```

2. `parse_output` 함수에서 파일 열기 전 `_validate_file_path(file_path)` 호출
3. `convert_format` 함수에서 출력 파일 쓰기 전 출력 경로도 동일하게 검증
4. `compute_ibo`, `compute_partial_charges`, `analyze_bonding`에 원자 수 제한 추가:

```python
def _validate_atom_spec(atom_spec: str, max_atoms: int = 200) -> str:
    """입력 분자 크기 제한."""
    lines = atom_spec.strip().splitlines()
    try:
        n_atoms = int(lines[0].strip())
        if n_atoms > max_atoms:
            raise ValueError(f"원자 수 {n_atoms}개가 최대 허용치 {max_atoms}개를 초과합니다.")
    except ValueError:
        # PySCF 직접 포맷: 세미콜론으로 구분
        n_atoms = len([l for l in atom_spec.split(';') if l.strip()])
        if n_atoms > max_atoms:
            raise ValueError(f"원자 수 {n_atoms}개가 최대 허용치 {max_atoms}개를 초과합니다.")
    return atom_spec
```

**테스트 추가: `tests/test_security.py`** (신규 파일)

- `test_path_traversal_blocked`: `../../../etc/passwd` 경로가 ValueError를 발생시키는지
- `test_allowed_path_works`: 프로젝트 내부 파일이 정상 동작하는지
- `test_atom_count_limit`: 201개 원자 입력 시 ValueError
- `test_atom_count_normal`: 200개 이하 정상 통과

### Phase δ-2: cube 해상도 + 국소화 검증 강화 [이슈 #8, #9]

**파일: `src/qcviz_mcp/backends/pyscf_backend.py`**

1. `generate_cube` 메서드의 기본 grid를 40에서 **80**으로 변경.
   파라미터 시그니처에 `nx=80, ny=80, nz=80` 추가하여 사용자가 조절 가능하게.
2. `compute_ibo` 메서드 반환값에 `localization_gradient` 키 추가:
   - PySCF의 `lo.ibo.ibo()` 호출 후 반환된 국소화 객체에서 최종 gradient norm을 추출
   - 1e-6 이상이면 `warnings` 리스트에 `"⚠️ 국소화가 안장점에 수렴했을 수 있습니다 (gradient={값}). Boys 또는 exponent=4로 재시도를 권장합니다."` 추가

**파일: `src/qcviz_mcp/tools/core.py`** 3. `compute_ibo` 도구의 반환 dict에 `warnings` 키 추가, 경고가 있으면 리스트로 반환

**테스트: `tests/test_pyscf_backend.py`** 4. `test_generate_cube_resolution_80`: 80³ grid로 생성된 cube 텍스트가 40³보다 큰지 확인 5. `test_localization_gradient_reported`: 국소화 결과에 gradient 값이 포함되는지 확인

### Phase δ-3: cc-pVDZ 벤치마크 확장 [이슈 #4]

**파일: `benchmark/molecules.py`**

1. 기존 10개 분자 각각에 `basis_sets: list = ["sto-3g", "cc-pVDZ"]` 필드 추가.
   (분자 정의는 동일, 기저함수만 확장)

**파일: `benchmark/run_benchmark.py`** 2. 이중 루프: 각 분자 × 각 기저함수로 벤치마크 실행 3. 결과 테이블에 기저함수 컬럼 추가 4. cc-pVDZ에서 시간과 IBO 개수가 sto-3g과 동일한지 비교

**파일: `tests/test_benchmark.py`** 5. cc-pVDZ 파라미터화 테스트 추가 (10개 분자 × cc-pVDZ)

- IBO 개수는 sto-3g과 동일해야 함 (기저함수와 무관)
- 전하 합 ≈ 0 (atol=1e-4)

### Phase δ-4: UHF 지원 기초 [이슈 #5]

**파일: `src/qcviz_mcp/backends/pyscf_backend.py`**

1. `compute_scf`에 UHF/UKS 분기 추가:

```python
if method_upper in ("UHF",):
    mf = scf.UHF(mol)
elif method_upper in ("UKS",):
    mf = scf.UKS(mol)
    mf.xc = xc_functional  # 기본 B3LYP
```

2. `compute_iao`에서 UHF인 경우 alpha/beta MO 계수를 별도 처리
   - `mo_occ > 0` 대신 `mf.mo_occ[0] > 0` (alpha), `mf.mo_occ[1] > 0` (beta) 분기
3. **주의**: IBO 국소화는 현재 alpha 궤도만 적용하고, beta는 별도 처리하거나 skip.
   지원 범위를 명시적으로 문서화.

**파일: `benchmark/molecules.py`** 4. O₂ (triplet, spin=2) 추가:

```python
{
    "name": "oxygen_triplet",
    "formula": "O₂",
    "atom_spec": "O 0.0 0.0 0.0; O 0.0 0.0 1.2075",
    "basis": "sto-3g",
    "spin": 2,
    "expected_n_ibo": None,  # UHF IBO 개수는 alpha/beta 별도
    "charge": 0,
    "feature": "Triplet ground state, UHF required"
}
```

**테스트: `tests/test_pyscf_backend.py`** 5. `test_compute_scf_uhf_o2`: O₂ triplet UHF 계산이 수렴하는지 확인 6. `test_uhf_not_supported_graceful`: UHF IBO가 아직 제한적이면 적절한 에러 메시지 반환 확인

### Phase δ-5: 실제 출력 파일 테스트용 fixture 생성 [이슈 #7]

**디렉토리: `tests/fixtures/`** (신규)

1. `tests/fixtures/water_hf_sto3g.out` 생성:
   - PySCF로 물 분자 HF/sto-3g 계산을 실행하고 출력을 캡처하여 텍스트 파일로 저장
   - 또는 cclib가 파싱할 수 있는 최소한의 PySCF molden 파일 생성
2. cclib의 내장 테스트 데이터 활용 가능 여부 확인:

   ```python
   import cclib
   # cclib.io.ccopen() 테스트에 사용할 수 있는 데이터 경로 확인
   ```

3. 실제 ORCA 출력이 없으므로, 최소한의 모의 ORCA 출력 fixture 생성:
   - `tests/fixtures/mock_orca_water.out`: ORCA 형식의 헤더 + 에너지 + 좌표를 포함한 텍스트
   - cclib가 이를 파싱할 수 있는지 검증

**테스트: `tests/test_parse_output.py`** (신규 또는 test_tools.py 확장) 4. `test_parse_real_pyscf_output`: fixture 파일을 `parse_output` 도구로 파싱 5. `test_parse_mock_orca_output`: 모의 ORCA 출력 파싱 6. `test_parse_nonexistent_file`: 존재하지 않는 파일 → FileNotFoundError 7. `test_parse_path_traversal_blocked`: 보안 테스트와 연동

### Phase δ-6: analyze_bonding 도구 검증 + 보강 [이슈 #14]

1. `src/qcviz_mcp/tools/core.py`의 `analyze_bonding` 함수를 확인:
   - 스텁이면 → 최소한의 실질 구현 추가 (IAO 전하 기반 결합 차수 추정, Wiberg bond order 가능하면)
   - 완전 구현이면 → 테스트만 추가
   - 구현이 불가능하면 → 도구 설명에 "experimental" 표시 + 반환값에 경고 포함

2. 테스트: `test_analyze_bonding_water` — 물 분자에서 O-H 결합이 감지되는지

### Phase δ-7: API 문서 자동 생성 [이슈 #11]

1. `mkdocs.yml` 생성 (프로젝트 루트):

```yaml
site_name: QCViz-MCP Documentation
site_url: https://dydtkddl.github.io/qcviz-mcp/
theme:
  name: material
plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
nav:
  - Home: index.md
  - API Reference:
      - Backends: api/backends.md
      - Tools: api/tools.md
      - MCP Server: api/server.md
  - Examples: examples.md
  - Changelog: changelog.md
```

2. `docs/index.md` — README.md 내용 기반 메인 페이지
3. `docs/api/backends.md` — mkdocstrings로 자동 생성:

```markdown
# Backend API Reference

::: qcviz_mcp.backends.pyscf_backend
::: qcviz_mcp.backends.cclib_backend
::: qcviz_mcp.backends.viz_backend
::: qcviz_mcp.backends.ase_backend
```

4. `docs/api/tools.md`, `docs/api/server.md` 동일 패턴
5. `docs/examples.md` — examples/ 스크립트 설명
6. pyproject.toml의 `[project.optional-dependencies]` docs 그룹에 `mkdocs-material`, `mkdocstrings[python]` 추가

### Phase δ-8: paper.md 본문 작성 [이슈 #2, #3, #10, #13]

**파일: `paper/paper.md`**

모든 JOSS 필수 섹션의 **실질 내용**을 작성한다. 저자가 나중에 수정할 수 있도록 `[TODO: 저자 확인 필요]` 마커를 포함한다.

1. **Summary** (~150 단어): QCViz-MCP가 무엇이고 왜 만들었는지.
2. **Statement of Need** (~300 단어): 현재 계산화학 워크플로우의 문제점, LLM 통합의 필요성, 기존 도구(IboView, Multiwfn)의 프로그래밍 제어 불가 문제.
3. **State of the Field** (~400 단어): ChemLint, El Agente, mcp.science/Globus Compute, VASPilot, ChatMol과의 비교. QCViz-MCP의 "궤도 국소화 + 시각화 특화 MCP"로서의 unique contribution.
4. **Software Design** (~400 단어): BackendRegistry 패턴, 선택적 의존성 아키텍처, Löwdin 정규직교화 선택 근거, FastMCP 3.x 채택 이유.
5. **Research Impact Statement** (~200 단어): 벤치마크 10개 분자(sto-3g + cc-pVDZ), E2E 파이프라인 검증, 재현 가능 튜토리얼, `[TODO: 외부 사용자 피드백 추가 예정]`.
6. **AI Usage Disclosure** (~200 단어):

```markdown
## AI Usage Disclosure

이 소프트웨어의 개발에 생성형 AI 도구가 사용되었습니다.

**사용된 도구**: Claude Code (Anthropic), Gemini 3.1 Pro (Google)

**사용 방식**:

- 코드 스캐폴딩 및 보일러플레이트 생성
- 디버깅 보조 (XYZ 파싱 에러, IAO 전하 수치 안정성 문제 해결)
- 테스트 케이스 초안 작성
- 문서 초안 작성

**인간 저자의 핵심 기여**:

- 프로젝트 아키텍처 설계 (Strategy F 하이브리드, BackendRegistry 패턴)
- IAO/IBO 알고리즘 선택 및 Löwdin 정규직교화 도입 결정
- JOSS/JCIM 논문 전략 수립
- 모든 계산 결과의 화학적 타당성 검증
- 벤치마크 분자 선정 및 기대값 설정

**검증 방법**:

- 모든 AI 생성 코드는 pytest 자동화 테스트로 검증 (현재 50+ 테스트)
- IAO 부분 전하 합이 중성 분자에서 0이 되는지 수치적으로 확인
- IBO 개수가 화학적 기대값과 일치하는지 10개 분자 벤치마크로 검증
  [TODO: 저자가 구체적 사용 내역을 확인하고 보완할 것]
```

7. **References**: paper.bib에 다음 추가:
   - Pan et al. (2025) Argonne MCP: arXiv:2508.18489
   - ChemLint: ChemRxiv 10.26434/chemrxiv.15000386
   - El Agente: DOI from Matter/Cell
   - Anthropic MCP specification (2024)
   - JOSS blog post on AI (2026-01-05)
   - Löwdin (1950) orthogonalization reference

### Phase δ-9: pyproject.toml 정비 + PROGRESS.md 업데이트

1. `pyproject.toml`:
   - `pythonpath = ["."]` 추가 (test_benchmark 수정)
   - `docs` 의존성에 `mkdocs-material>=9.0`, `mkdocstrings[python]>=0.24` 추가
   - 버전을 `0.3.0-alpha`로 올림
2. `PROGRESS.md` 전면 업데이트:
   - 전수조사 18건 이슈 대응 현황
   - 현재 테스트 수, 커버리지 추정
   - 다음 마일스톤
3. `CHANGELOG.md`에 v0.3.0-alpha 항목 추가

### Phase δ-10: 최종 검증 + Git

1. `ruff check src/ tests/ --fix` 실행
2. `ruff format src/ tests/` 실행
3. `pytest -v` 실행 → 전체 통과 확인
   - 예상 테스트 수: 기존 50 + 보안 4 + 해상도 2 + cc-pVDZ 벤치마크 10 + UHF 2 + 파싱 4 + analyze_bonding 1 = **~73개**
4. `pytest --cov=qcviz_mcp --cov-report=term-missing` → 커버리지 확인
5. Git:

```bash
git add -A
git commit -m "feat: Phase δ — 전수조사 18건 대응 (보안, 해상도, cc-pVDZ, UHF, API docs, paper)"
git tag -a v0.3.0-alpha -m "v0.3.0-alpha: security patches, cc-pVDZ benchmarks, UHF support, API docs, JOSS paper draft"
git push origin main --tags
```

## CONSTRAINTS

1. **기존 50개 테스트를 절대 깨뜨리지 마라.** 새 코드 추가 시 기존 테스트 전부 통과 확인.
2. **보안 패치(δ-1)를 가장 먼저 실행하라.** 이후 Phase는 보안 패치가 적용된 상태에서 진행.
3. **cc-pVDZ 벤치마크 실행 시 시간이 오래 걸릴 수 있다.** 분자당 30초 이상이면 timeout 설정.
4. **UHF IBO가 완벽하게 동작하지 않을 수 있다.** 실패 시 graceful하게 처리하고 "experimental" 표시.
5. **paper.md는 한국어가 아닌 영어로 작성하라** (JOSS 제출용).
6. **mkdocs 빌드를 시도하되, 실패해도 Phase를 멈추지 마라.** 설정 파일만 올바르면 OK.
7. **모든 파일에 UTF-8 인코딩을 사용하라.**
8. **각 Phase 시작 전 현재 디렉토리가 프로젝트 루트인지 확인하라.**
9. **생성하는 모든 테스트 파일 상단에 `# -*- coding: utf-8 -*-`를 포함하라.**
10. **실패한 Phase는 `PROGRESS.md`에 실패 사유를 기록하고 다음 Phase로 넘어가라.**

## OUTPUT

모든 Phase 완료 후 **한 번만** 다음 형식으로 최종 보고:

```
═══ QCViz-MCP Phase δ 최종 보고 ═══

Phase δ-1 (보안): ✅/❌ — [요약]
Phase δ-2 (해상도): ✅/❌ — [요약]
Phase δ-3 (cc-pVDZ): ✅/❌ — [요약]
Phase δ-4 (UHF): ✅/❌ — [요약]
Phase δ-5 (파싱): ✅/❌ — [요약]
Phase δ-6 (bonding): ✅/❌ — [요약]
Phase δ-7 (API docs): ✅/❌ — [요약]
Phase δ-8 (paper): ✅/❌ — [요약]
Phase δ-9 (config): ✅/❌ — [요약]
Phase δ-10 (검증): ✅/❌ — [요약]

테스트: X passed / Y failed / Z skipped
커버리지: X%
Git: commit [hash], tag v0.3.0-alpha
남은 사람 작업: [목록]
```

## START

docs/ 디렉토리를 읽고 현재 코드 상태를 파악한 뒤,
Phase δ-1부터 δ-10까지 순서대로 전부 실행하시오.
중간에 멈추지 마라. 끝까지 가라.
